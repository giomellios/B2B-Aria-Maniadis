// TEMPORARY: deliberate formatting violation to verify the CI format gate.
// This file is type-clean and lint-clean, but not Prettier-formatted.
// Remove once the pipeline has been confirmed to catch it.
export const ciPipelineCheck    =    "intentionally badly formatted";

export function ciPipelineSum( a: number,b: number ) {
      return a+b;
}
